<form method="post" action="{{ path('students_payement_methods_delete', {'id': students_payement_method.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ students_payement_method.id) }}">
    <button class="btn">Delete</button>
</form>
