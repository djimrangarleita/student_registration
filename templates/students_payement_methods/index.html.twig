{% extends 'base.html.twig' %}

{% block title %}StudentsPayementMethods index{% endblock %}

{% block body %}
    <h1>StudentsPayementMethods index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bank_details</th>
                <th>Card_details</th>
                <th>Other_details</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for students_payement_method in students_payement_methods %}
            <tr>
                <td>{{ students_payement_method.id }}</td>
                <td>{{ students_payement_method.bankDetails }}</td>
                <td>{{ students_payement_method.cardDetails }}</td>
                <td>{{ students_payement_method.otherDetails }}</td>
                <td>
                    <a href="{{ path('students_payement_methods_show', {'id': students_payement_method.id}) }}">show</a>
                    <a href="{{ path('students_payement_methods_edit', {'id': students_payement_method.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('students_payement_methods_new') }}">Create new</a>
{% endblock %}
