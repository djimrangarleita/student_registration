{% extends 'base.html.twig' %}

{% block title %}StudentsPayementMethods{% endblock %}

{% block body %}
    <h1>StudentsPayementMethods</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ students_payement_method.id }}</td>
            </tr>
            <tr>
                <th>Bank_details</th>
                <td>{{ students_payement_method.bankDetails }}</td>
            </tr>
            <tr>
                <th>Card_details</th>
                <td>{{ students_payement_method.cardDetails }}</td>
            </tr>
            <tr>
                <th>Other_details</th>
                <td>{{ students_payement_method.otherDetails }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('students_payement_methods_index') }}">back to list</a>

    <a href="{{ path('students_payement_methods_edit', {'id': students_payement_method.id}) }}">edit</a>

    {{ include('students_payement_methods/_delete_form.html.twig') }}
{% endblock %}
