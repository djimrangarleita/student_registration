{% extends 'base.html.twig' %}

{% block title %}StudentsRelationships{% endblock %}

{% block body %}
    <h1>StudentsRelationships</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ students_relationship.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('students_relationships_index') }}">back to list</a>

    <a href="{{ path('students_relationships_edit', {'id': students_relationship.id}) }}">edit</a>

    {{ include('students_relationships/_delete_form.html.twig') }}
{% endblock %}
