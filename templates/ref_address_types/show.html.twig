{% extends 'base.html.twig' %}

{% block title %}RefAddressTypes
{% endblock %}

{% block body %}
	<div class="container">
		<h1>RefAddressTypes</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ ref_address_type.id }}</td>
				</tr>
				<tr>
					<th>Address_type_code</th>
					<td>{{ ref_address_type.addressTypeCode }}</td>
				</tr>
				<tr>
					<th>Address_type_description</th>
					<td>{{ ref_address_type.addressTypeDescription }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('ref_address_types_index') }}">back to list</a>

		<a href="{{ path('ref_address_types_edit', {'id': ref_address_type.id}) }}">edit</a>

		{{ include('ref_address_types/_delete_form.html.twig') }}
	</div>
{% endblock %}
