<form method="post" action="{{ path('ref_relationship_types_delete', {'id': ref_relationship_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ref_relationship_type.id) }}">
    <button class="btn">Delete</button>
</form>
