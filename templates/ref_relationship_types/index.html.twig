{% extends 'base.html.twig' %}

{% block title %}RefRelationshipTypes index{% endblock %}

{% block body %}
    <h1>RefRelationshipTypes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Relationship_type_code</th>
                <th>Relation_type_description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ref_relationship_type in ref_relationship_types %}
            <tr>
                <td>{{ ref_relationship_type.id }}</td>
                <td>{{ ref_relationship_type.relationshipTypeCode }}</td>
                <td>{{ ref_relationship_type.relationTypeDescription }}</td>
                <td>
                    <a href="{{ path('ref_relationship_types_show', {'id': ref_relationship_type.id}) }}">show</a>
                    <a href="{{ path('ref_relationship_types_edit', {'id': ref_relationship_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ref_relationship_types_new') }}">Create new</a>
{% endblock %}
