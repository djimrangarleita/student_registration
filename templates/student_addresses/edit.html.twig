{% extends 'base.html.twig' %}

{% block title %}Edit StudentAddresses
{% endblock %}

{% block body %}
	<div class="container">

		<h1>Edit StudentAddresses</h1>

		{{ include('student_addresses/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('student_addresses_index') }}">back to list</a>

		{{ include('student_addresses/_delete_form.html.twig') }}
	</div>
{% endblock %}
